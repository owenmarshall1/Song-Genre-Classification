# Song-Genre-Classification
Our goal is to train a neural network that takes audio data from a music track and predicts what genre the song is. This is a multi-class classification problem, using supervised learning on a labeled dataset of songs.
